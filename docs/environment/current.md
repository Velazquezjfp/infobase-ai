# Environment Variables — Current State

_Generated by BO-env-documenter on 2026-04-27. Do not edit manually — changes will be overwritten._

**Legend:** 🔒 = sensitive (treat value as secret)

## Runtime

### `LOG_LEVEL`
- **Required:** no (default: `INFO`)
- **Type:** string
- **Used by:** `backend/config.py`
- **Example value:** `INFO`
- **Purpose:** application log verbosity (consumed by Python `logging` configuration)

---

## VITE

### `VITE_API_BASE_URL`
- **Required:** no (default: `http://localhost:8000`)
- **Type:** URL
- **Used by:** `src/components/workspace/SubmitCaseDialog.tsx`, `src/components/workspace/DocumentViewer.tsx`, `src/lib/adminApi.ts`, `src/contexts/AppContext.tsx`
- **Example value:** _(none — no .env.example present)_
- **Purpose:** backend API base URL for the Vite frontend (used by submit-case, document viewer, admin API client, and global app context)
- **Warning:** duplicate purpose with `VITE_API_URL` — different frontend modules read different names with the same fallback. Consolidate to a single env var to avoid drift.

### `VITE_API_URL`
- **Required:** no (default: `http://localhost:8000`)
- **Type:** URL
- **Used by:** `src/components/workspace/AdminConfigPanel.tsx`, `src/components/workspace/AIChatInterface.tsx`, `src/components/workspace/CaseContextDialog.tsx`, `src/components/workspace/CaseTreeExplorer.tsx`, `src/components/workspace/ContextHierarchyDialog.tsx`, `src/lib/fileApi.ts`
- **Example value:** _(none — no .env.example present)_
- **Purpose:** backend API base URL for the Vite frontend (used by admin panel, chat interface, case dialogs, tree explorer, and file API client)
- **Warning:** duplicate purpose with `VITE_API_BASE_URL` — see note above.

---

## Miscellaneous

### `DOCUMENTS_PATH`
- **Required:** no (default: `public/documents`)
- **Type:** string (filesystem path)
- **Used by:** `backend/config.py` (`get_documents_path`)
- **Example value:** _(none — no .env.example present)_
- **Purpose:** base path for document storage; in containerised deployments set to `/var/app/documents` and mount as a persistent volume

### 🔒 `GEMINI_API_KEY`
- **Required:** **yes** (no default; services raise `ValueError` when missing)
- **Type:** string
- **Used by:** `backend/services/gemini_service.py` (`_initialize_model`), `backend/services/validation_service.py` (`_initialize_model`), `backend/services/shacl_generator.py` (singleton factory)
- **Example value:** `sensitive — see secrets manager or .env.example`
- **Purpose:** Google Gemini API key used to instantiate generative models for chat, validation, and SHACL shape generation

### `IDIRS_BASE_URL`
- **Required:** no (default: `http://localhost:8010`)
- **Type:** URL
- **Used by:** `backend/config.py`
- **Example value:** _(none — no .env.example present)_
- **Purpose:** base URL of the IDIRS service used for hybrid search and RAG retrieval

### `INIT_TEST_DOCS`
- **Required:** no (default: `false`)
- **Type:** boolean
- **Used by:** `backend/main.py` (lifespan startup)
- **Example value:** _(none — no .env.example present)_
- **Purpose:** when set to `true`, auto-initialises the test document set for `ACTE-2024-001` on backend startup

### `SKIP_INTEGRATION_TESTS`
- **Required:** no (default: `1`)
- **Type:** boolean (string `"1"`/`"0"`)
- **Used by:** `backend/tests/test_anonymization.py`
- **Example value:** _(none — no .env.example present)_
- **Purpose:** when `1`, skips integration tests that depend on the external anonymization service
