# Environment Changelog — 2026 Q2

## 2026-04-27

_Genesis entry — first run of BO-env-documenter. All currently-documented vars are listed as Added._

### Added
- `LOG_LEVEL` — used in `backend/config.py`. Optional, default `INFO`. Type: string. Application log verbosity.
- `VITE_API_BASE_URL` — used in `src/components/workspace/SubmitCaseDialog.tsx`, `src/components/workspace/DocumentViewer.tsx`, `src/lib/adminApi.ts`, `src/contexts/AppContext.tsx`. Optional, default `http://localhost:8000`. Type: URL. Frontend → backend API base URL.
- `VITE_API_URL` — used in `src/components/workspace/AdminConfigPanel.tsx`, `src/components/workspace/AIChatInterface.tsx`, `src/components/workspace/CaseContextDialog.tsx`, `src/components/workspace/CaseTreeExplorer.tsx`, `src/components/workspace/ContextHierarchyDialog.tsx`, `src/lib/fileApi.ts`. Optional, default `http://localhost:8000`. Type: URL. Frontend → backend API base URL.
- `DOCUMENTS_PATH` — used in `backend/config.py::get_documents_path`. Optional, default `public/documents`. Type: filesystem path. Document storage base path.
- `GEMINI_API_KEY` (sensitive) — used in `backend/services/gemini_service.py::_initialize_model`, `backend/services/validation_service.py::_initialize_model`, `backend/services/shacl_generator.py`. Required (raises `ValueError` if missing). Type: string. Google Gemini API key.
- `IDIRS_BASE_URL` — used in `backend/config.py`. Optional, default `http://localhost:8010`. Type: URL. IDIRS hybrid-search/RAG service base URL.
- `INIT_TEST_DOCS` — used in `backend/main.py`. Optional, default `false`. Type: boolean. Auto-initialise test documents on startup.
- `SKIP_INTEGRATION_TESTS` — used in `backend/tests/test_anonymization.py`. Optional, default `1`. Type: boolean. Skip anonymization integration tests.

### Notes
- Duplicate-purpose pair detected: `VITE_API_URL` and `VITE_API_BASE_URL` carry the same default (`http://localhost:8000`) and resolve the same intent (frontend backend URL) but are read by different files. Recommend consolidating to a single name.
- No `.env.example` (or `.env.sample` / `.env.template` / `.env.dist`) was found at the project root; example values could not be enriched. Only `.env` (forbidden) and `backend/.env` (forbidden) were present.

## 2026-04-28

### Added
- `LLM_BACKEND` — used in `backend/config.py` (consumed by `backend/services/llm_provider.py::get_provider()`). Optional, default `internal`. Enum (`internal` | `external`); any other value raises `ValueError`. Introduced by S001-F-001.
- `LITELLM_PROXY_URL` — used in `backend/config.py`. Optional, default `''` (empty); effectively required when `LLM_BACKEND=internal`. Type URL. Introduced by S001-F-001.
- `LITELLM_TOKEN` (sensitive) — used in `backend/config.py`. Optional, default `''` (empty). Type string. Introduced by S001-F-001.
- `LITELLM_MODEL` — used in `backend/config.py`. Optional, default `gemma3:12b`. Type string. Introduced by S001-F-001.
- `DEV` — used in `src/i18n/config.ts`. Vite built-in (not user-set); type boolean. Toggles i18next debug logging. Introduced by S001-F-008.

### Modified
- `GEMINI_API_KEY`: was previously documented as **required** (services raised `ValueError` when missing). After the S001-F-001 refactor, the variable is now centralised in `backend/config.py` via `os.getenv('GEMINI_API_KEY', '')`. Status changed to **optional, default `''` empty**, but **effectively required when `LLM_BACKEND=external`**. The "Used by" list also changed: previously listed three service modules (`gemini_service.py`, `validation_service.py`, `shacl_generator.py`); the centralised reference now lives in `backend/config.py` and is consumed via `backend/services/llm_provider.py::GeminiProvider`. Sensitive flag unchanged (still sensitive).
- `DOCUMENTS_PATH`: example value populated from new `.env.example` (`public/documents`). No code change.
- `IDIRS_BASE_URL`: example value populated from new `.env.example` (`http://localhost:8010`). No code change.
- `INIT_TEST_DOCS`: example value populated from new `.env.example` (`false`). No code change.
- `SKIP_INTEGRATION_TESTS`: example value populated from new `.env.example` (`1`). No code change.
- `LOG_LEVEL`: example value populated from new `.env.example` (`INFO`). No code change.
- `VITE_API_URL` / `VITE_API_BASE_URL`: note clarified — `.env.example` now exists in the repo (introduced by S001-D-001) but does not declare these two frontend keys; both still rely on the `http://localhost:8000` fallback at runtime.

## 2026-04-28

### Modified
- `LITELLM_PROXY_URL`: required-status semantics tightened. Config-layer default unchanged (`''` via `os.getenv('LITELLM_PROXY_URL', '')`), but `backend/services/llm_provider.py::_build_provider` (added by S001-NFR-004) now raises `ValueError("LITELLM_PROXY_URL is required when LLM_BACKEND=internal — start the LiteLLM container per S001-NFR-004 or set LLM_BACKEND=external")` before any provider is instantiated when `LLM_BACKEND=internal` and the URL is empty/blank. Previously the failure surfaced silently at first request time. Doc entry now also lists `backend/services/llm_provider.py::_build_provider` under "Used by".
- `GEMINI_API_KEY`: doc entry sharpened — explicit `ValueError("GEMINI_API_KEY not configured for external backend")` from `GeminiProvider.__init__` (S001-F-001 behaviour, previously documented loosely as "the Gemini provider will fail to initialise"). No code change in this wave; documentation parity with the `LITELLM_PROXY_URL` precondition.
- `LITELLM_TOKEN`: doc entry now cross-references `litellm/.env::LITELLM_MASTER_KEY` (introduced by S001-NFR-004) — the two values must match for the proxy to authenticate backend requests. No code change; documentation linkage only.
- `LLM_BACKEND`: example value in doc updated from `external` to `internal` to match the `.env.example` cutover landed by S001-NFR-004 (the safe default is now the closed-environment path because the proxy ships as a local container). Code-side default in `backend/config.py` was already `internal` since S001-F-001; no code change in this wave.

## 2026-04-29

### Added
- `ENABLE_ANONYMIZATION` — used in `backend/config.py`. Optional, default `false`. Boolean (parsed via `get_bool_env`). Gates the PII anonymization service path (S001-F-004); when `false`, the app shows a user-facing notice instead of calling the unreachable external service. Backfill: was already present in code/.env.example but missing from prior `current.md` snapshot.
- `ENABLE_DOCUMENT_SEARCH` — used in `backend/config.py` (consumed by `backend/api/idirs.py` and `backend/api/search.py`). Optional, default `false`. Boolean (parsed via `get_bool_env`). S001-F-005 wired the flag into both `POST /api/idirs/search` and `POST /api/idirs/rag` (early-return 503 + `feature_disabled` body before any outbound IDIRS connection) and surfaces it in `GET /api/search/health` as `document_search: "enabled"|"disabled"`. Backfill: was already in code/.env.example but missing from prior `current.md` snapshot.
- `ENABLE_UPLOAD` — used in `backend/config.py`. Optional, default `false`. Boolean (parsed via `get_bool_env`). Gates the file-upload subsystem (S001-F-006); stubbed out for sprint 1. Backfill: was already in code/.env.example but missing from prior `current.md` snapshot.

## 2026-04-29 — genesis (full rebuild)

Genesis run of the env-doc generator in **full mode**. All 16 env vars discovered in the code graph (`env_var` entities + `reads_env` relations) are recorded as added in this entry. Subsequent runs will diff against `current.md` and only list real changes.

### Added
- `DEV` — used in `src/i18n/config.ts`. Vite built-in (no user-set value); toggles i18next debug logging.
- `DOCUMENTS_PATH` — used in `backend/config.py`. Optional, default `public/documents`.
- 🔒 `GEMINI_API_KEY` — used in `backend/config.py` (consumed by `backend/services/llm_provider.py::GeminiProvider`). Optional at config layer (default `""`), required when `LLM_BACKEND=external`.
- `IDIRS_BASE_URL` — used in `backend/config.py`. Optional, default `http://localhost:8010`.
- `INIT_TEST_DOCS` — used in `backend/main.py`. Optional, default `false`.
- `LITELLM_MODEL` — used in `backend/config.py`. Optional, default `gemma3:12b`.
- `LITELLM_PROXY_URL` — used in `backend/config.py` (validated in `backend/services/llm_provider.py::LiteLLMProvider`). Optional at config layer (default `""`), required when `LLM_BACKEND=internal`.
- 🔒 `LITELLM_TOKEN` — used in `backend/config.py`. Optional, default `""` (empty string acceptable for unauthenticated proxies).
- `LLM_BACKEND` — used in `backend/config.py` (consumed by `backend/services/llm_provider.py::get_provider()`). Optional, default `internal`.
- `LOG_LEVEL` — used in `backend/config.py`. Optional, default `INFO`.
- `SKIP_INTEGRATION_TESTS` — used in `backend/tests/test_anonymization.py`. Optional, default `"1"`.
- `VITE_API_BASE_URL` — used in `src/components/workspace/DocumentViewer.tsx`, `src/components/workspace/SubmitCaseDialog.tsx`, `src/contexts/AppContext.tsx`, `src/lib/adminApi.ts`. Optional, default `http://localhost:8000`.
- `VITE_API_URL` — used in `src/components/workspace/AIChatInterface.tsx`, `src/components/workspace/AdminConfigPanel.tsx`, `src/components/workspace/CaseContextDialog.tsx`, `src/components/workspace/CaseTreeExplorer.tsx`, `src/components/workspace/ContextHierarchyDialog.tsx`, `src/lib/fileApi.ts`. Optional, default `http://localhost:8000`.
- `ENABLE_ANONYMIZATION` — used in `backend/config.py` (S001-F-004). Optional, default `false`.
- `ENABLE_DOCUMENT_SEARCH` — used in `backend/config.py` (consumed by `backend/api/idirs.py` and `backend/api/search.py`, S001-F-004 / S001-F-005). Optional, default `false`.
- `ENABLE_UPLOAD` — used in `backend/config.py` (consumed by `backend/api/files.py::upload_file` and `backend/api/files.py::files_health_check`, S001-F-006). Optional, default `false`.

### Notes
- Sensitive vars: `GEMINI_API_KEY`, `LITELLM_TOKEN` — example values intentionally elided.
- Duplicate-purpose pair: `VITE_API_URL` and `VITE_API_BASE_URL` both fall back to `http://localhost:8000`. Tracked for consolidation under F-009.
- `wave-5-summary-S001-F-006.md` notes that `ENABLE_UPLOAD` is now also consumed by `backend/api/files.py`; the graph at the time of this run only records the `backend/config.py` definition site, but source inspection confirms `config.ENABLE_UPLOAD` is read at `backend/api/files.py:126` and `:606`. Rerun `/code-graph-update` to capture the additional `reads_env` relations.

## 2026-04-29

### Added
- `ENABLE_CHAT_HISTORY` — used in `backend/config.py`, consumed by `backend/services/gemini_service.py` and `backend/api/chat.py`. Optional, default `false`. Boolean (parsed via `get_bool_env`). Feature flag for persistent per-case conversation history.
- `MAX_CONVERSATION_HISTORY` — used in `backend/config.py`, consumed by `backend/services/conversation_manager.py`. Optional, default `10`. Integer. Caps prior-message retention per case.
- `MAX_TOKENS_PER_REQUEST` — used in `backend/config.py`, consumed by `backend/services/conversation_manager.py`. Optional, default `30000`. Integer. Token-budget ceiling per LLM request.
- `TOKEN_ESTIMATE_PER_CHAR` — used in `backend/config.py`, consumed by `backend/services/conversation_manager.py`. Optional, default `0.25`. Float. Heuristic for token-count estimation from character length.
- `RESERVE_TOKENS` — used in `backend/config.py`, consumed by `backend/services/conversation_manager.py`. Optional, default `5000`. Integer. Tokens reserved for the current prompt/response, subtracted from `MAX_TOKENS_PER_REQUEST`.
- `IDIRS_TIMEOUT` — used in `backend/config.py`, consumed by `backend/api/idirs.py`. Optional, default `30`. Integer (seconds). HTTP timeout for IDIRS hybrid-search and RAG calls.
- `RAG_CONFIDENCE_THRESHOLD` — used in `backend/config.py`, consumed by `backend/api/idirs.py`. Optional, default `0.80`. Float. Confidence cut-off above which RAG answers are treated as well-supported.
- `SESSION_IDLE_TIMEOUT_MINUTES` (S001-F-007) — used in `backend/config.py`. Optional, default `10`. Integer (minutes). Backend-side reference for the ephemeral-session idle reset; surfaced in the configuration summary. Also documented in `.env.example` with the same default.
- `VITE_SESSION_IDLE_TIMEOUT_MINUTES` (S001-F-007) — used in `src/hooks/useIdleTimeout.ts` (read via `import.meta.env`). Optional, default `10`. Integer (minutes). Frontend counterpart for the idle-reset window; should match the backend value.
- 🔒 `LITELLM_MASTER_KEY` (sensitive; S001-NFR-004) — read by the LiteLLM proxy container (`litellm/config.yaml::general_settings.master_key`) and by integration tests `docs/tests/S001-NFR-004/TC-S001-NFR-004-{04,05,08}.py`. Required for the proxy to authenticate; lives in the gitignored `litellm/.env` (not the app `.env`). Must be set to the same string as the backend's `LITELLM_TOKEN`.

### Modified
- `DOCUMENTS_PATH`: usage list extended — also read at module-import time by `backend/services/session_manager.py` via the `DOCUMENTS_BASE_PATH` indirection (S001-F-007). No type, default, required, or sensitive flag change.
- `INIT_TEST_DOCS`: purpose annotation updated — `backend/main.py::lifespan` now runs `reset_case_to_seed("ACTE-2024-001")` first; the legacy `INIT_TEST_DOCS=true` block is kept as a documented no-op back-compat path. No type, default, required, or sensitive flag change.
- `IDIRS_BASE_URL`: usage list extended — explicitly documents `backend/api/idirs.py` as a consumer (was previously listed only as `backend/config.py`). No type, default, required, or sensitive flag change.
- `VITE_API_BASE_URL`: purpose annotation updated — also used for the `POST /api/session/reset` call from the S001-F-007 logout / `beforeunload` / idle-timeout flows in `src/contexts/AppContext.tsx`. No type, default, required, or sensitive flag change.
